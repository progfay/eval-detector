version: "3"
services:
  chrome:
    image: chromedp/headless-shell:latest
    ports:
      - "9222:9222"

  node:
    build: ./
    ports:
      - "8080:8080"
    depends_on:
      - chrome
    command: npm start
